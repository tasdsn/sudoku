表示
    1.画面に9✖️9マスの画面を作る
    2.一つ一つはセレクトボックスで値を選択できる
    3.初期値が決まっているところは固定
    4.処理を開始ボタンを表示
ルール
    1.同じ列には同じ数字は入らない
    2.同じ行には同じ数字は入らない
    3.9個一つでブロックになりその中には同じ数字は入らない
構成
    1.縦の何列目かのクラス、横の何行目かのクラス、どこのボックスかのクラスを作る。
処理
    固定値を入れて、処理を開始ボタンを押すと自動的に計算する